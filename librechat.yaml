version: "1.0.0"

endpoints:
  openAI:
    baseURL: "https://api.openai.com/v1"
    apiKey: "${OPENAI_API_KEY}"
    modelOptions:
      model: "gpt-4.1-mini"
      useResponsesApi: true
      text: true
      truncation: auto

